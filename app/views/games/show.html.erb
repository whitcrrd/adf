<h3>Game Show Page</h3>
<!-- display both users and their teams -->

<% @teams.each do |team| %>
  <%= team.name %>

<% end %>

<%= link_to 'Join This Game', new_game_team_path(@game.id), game_id: @game.id %>